#include "main.h"

/**
 * _strlen - a function that returns the length of a string
 * @s: string
 *
 * Return: 0
 */

int _strlen(char *s)
{
	/* c is used as counter */

	int c;

	for (c = 0; s[c] != '\0'; c++)
	{
		continue;
	}
	return (c);
}

